export interface Customer {
  id: number;
  name: string;
  code: string;
  address: string;
  areaCode: string;
  categoryCode: string;
}

export interface CustomerDropDown {
  code: string;
  name: string;
}
